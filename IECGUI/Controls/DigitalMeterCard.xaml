<UserControl x:Class="IECGUI.Controls.DigitalMeterCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IECGUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="250">
    <Border CornerRadius="10">

        <Border.Background>
            <LinearGradientBrush>
                <GradientStop Color="#2E3440" Offset="0"/>
                <GradientStop Color="#1A1F2B" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <TextBlock FontSize="48" FontFamily="digital-7" FontWeight="Bold"  HorizontalAlignment="Center"
                   VerticalAlignment="Center" 
                   Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl},Path=Value,StringFormat={}{0:F2}}"
                   Foreground="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=CurrentBrush}" />

    </Border>
</UserControl>
